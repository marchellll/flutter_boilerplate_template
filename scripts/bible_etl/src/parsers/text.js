/**
 * Parses plain text format
 */
function parseText(sourceDir, source) {
  // Basic text parser - implement based on your text format
  const data = { books: new Map(), chapters: new Map(), verses: [] };
  // Implementation depends on specific text format structure
  return data;
}

module.exports = { parseText };
